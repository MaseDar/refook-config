server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MaseDar/refook-config
          clone-on-start: true
  security:
    user:
      name: config
      password: password